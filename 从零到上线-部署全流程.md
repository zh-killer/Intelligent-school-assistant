# 🌐 从零到上线 — 服务器购买 & 部署全流程

> 写给第一次部署项目的你。不讲概念，只讲实操。

---

## 一、先说人话：到底什么是"买服务器"

### 1.1 你就当是租了一台远程电脑

```
你现在：代码在自己电脑上跑 → 只能自己打开 http://127.0.0.1:7860

买服务器后：代码在云厂商的电脑上跑 → 任何人都能通过网址访问
```

买服务器 = 在阿里云/腾讯云租一台**24 小时不关机的电脑**，把你的代码放上去跑。

### 1.2 三种常见形式

| 类型 | 比喻 | 价格 | 适合 |
|------|------|------|------|
| **云服务器 ECS** | 租一整台电脑 | 50-200元/月 | 个人项目首选 |
| **轻量应用服务器** | 带装修的电脑（系统+环境预装） | 40-100元/月 | 新手最推荐 |
| **Serverless 函数计算** | 只租厨房，不做饭不花钱 | 按调用量计费 | 极小项目 |

**你现阶段买轻量应用服务器就够了**，下面讲这个。

---

## 二、买服务器（10 分钟搞定）

### 2.1 选哪家

```
🥇 阿里云   — 市场份额最大，文档最全，学生免费额度
🥈 腾讯云   — 微信小程序天然配合，香港节点免备案
🥉 华为云   — 也还行
```

### 2.2 配置选什么

| 配置项 | 选什么 | 原因 |
|--------|--------|------|
| **CPU** | 2 核 | 1核跑 AI 应用会炸 |
| **内存** | 2G-4G | 4G 稳妥，2G 勉强 |
| **硬盘** | 40G-60G | 代码+数据库+日志够用 |
| **系统** | Ubuntu 22.04 / CentOS 7.9 | Ubuntu 新手友好 |
| **带宽** | 3M-5M | 个人够用 |
| **地域** | 离你最近的 | 选北京/上海/广州 |

### 2.3 价格

```
阿里云轻量应用服务器：
  2核2G  →  约 50-70 元/月
  2核4G  →  约 90-120 元/月

新用户第一年通常有优惠：
  2核2G  →  约 300-500 元/年
```

### 2.4 购买流程（阿里云为例）

```
① 打开 aliyun.com → 注册/登录 → 实名认证（上传身份证）
② 搜"轻量应用服务器" → 点"立即购买"
③ 选配置：
   - 镜像选 Ubuntu 22.04（别选 Windows，贵一倍）
   - 地域选离你最近的
   - 时长先买 1 个月试水
④ 付款 → 等 1 分钟 → 进入控制台
⑤ 点"远程连接" → 你就看到一台 Linux 的命令行界面了
```

---

## 三、把项目部署上去（你的两个项目为例）

### 3.1 基础流程（所有项目通用）

```
你本地电脑                       云服务器
─────────                      ─────────

① git push 到 GitHub             ④ git pull 拉代码
② ssh 连上服务器                  ⑤ 装依赖 pip install -r requirements.txt
③ 装运行环境（Python、Java等）    ⑥ 启动项目
                                 ⑦ 配置 24 小时不关闭
```

### 3.2 部署你的智慧校园 AI 项目

```bash
# ---- 在服务器上执行 ----

# 1. 更新系统 + 装 Python
sudo apt update
sudo apt install python3 python3-pip git -y

# 2. 拉代码
git clone https://github.com/zh-killer/-Life-Equation.git
cd Life-Equation

# 3. 装依赖
pip install -r requirements.txt

# 4. 创建 .env 文件，填入真实密钥
nano .env
# 粘贴你的 zhipuai_api_key=xxx
# Ctrl+X 保存退出

# 5. 后台启动（关了终端也不停）
nohup python "智能校园助手ai.py" > app.log 2>&1 &

# 6. 现在访问 http://你的服务器IP:7860 就能打开了
```

**问题**：这样启动，服务器重启后程序就没了。生产环境需要用 `systemd`：

```bash
# 创建一个系统服务（开机自启+崩溃自动重启）
sudo nano /etc/systemd/system/smart-campus.service
```

```ini
[Unit]
Description=Smart Campus AI
After=network.target

[Service]
User=root
WorkingDirectory=/root/Life-Equation
ExecStart=/usr/bin/python3 "智能校园助手ai.py"
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload        # 加载配置
sudo systemctl start smart-campus    # 启动
sudo systemctl enable smart-campus   # 开机自启
sudo systemctl status smart-campus   # 查看状态
```

### 3.3 部署你的生命方程式小程序后端（Spring Boot）

```bash
# 1. 装 Java
sudo apt install openjdk-17-jdk -y

# 2. 装 Maven
sudo apt install maven -y

# 3. 打包（在本地或服务器上）
cd 你的SpringBoot项目
mvn clean package -DskipTests
# 会在 target/ 下生成 life_equation-0.0.1.jar

# 4. 传到服务器上运行
nohup java -jar target/life_equation-0.0.1.jar > app.log 2>&1 &
```

同样建议配成 systemd 服务。

---

## 四、微信小程序特别注意

```
你现在的状态：
  微信小程序 → 前端请求 → http://localhost:8888/api
                              ↑
                        这是你本机，别人访问不到

需要改成：
  微信小程序 → 前端请求 → https://你的域名.com/api
                              ↑
                        这是云服务器上的 Spring Boot
```

**关键限制**：微信小程序要求后端接口必须是 **HTTPS + 备案域名**，不能用 IP 地址。

### 所以你需要额外做：

| 步骤 | 做什么 | 耗时 | 花费 |
|------|--------|------|------|
| ① 买域名 | 阿里云/腾讯云买一个 .com/.cn | 10分钟 | 30-60元/年 |
| ② 备案 | 提交身份证、填表、等审核 | 15-20天 | 免费 |
| ③ SSL证书 | 申请免费 HTTPS 证书 | 10分钟 | 免费 |
| ④ 配 DNS | 把域名指向服务器 IP | 10分钟 | 免费 |

**香港节点的优势**：如果用香港服务器，可以免备案。

---

## 五、域名 & HTTPS 怎么搞

### 5.1 买域名

```
① 打开阿里云 → 搜"域名注册"
② 输入你想要的域名 → 查可用性
③ .com 约 60元/年  .cn 约 30元/年  .top 约 9元/年
④ 付款 → 实名认证 → 完成
```

### 5.2 把域名指向你的服务器

```
域名注册商后台 → DNS 解析 → 添加记录：

  记录类型: A
  主机记录: @         （即 your-domain.com）
  记录值:   你的服务器 IP 地址

  记录类型: A
  主机记录: www       （即 www.your-domain.com）
  记录值:   你的服务器 IP 地址
```

配置完后等 10 分钟，浏览器输入你的域名就能访问服务器了。

### 5.3 免费 HTTPS 证书

```bash
# 服务器上安装 certbot
sudo apt install certbot python3-certbot-nginx -y

# 一键申请免费 SSL 证书（Let's Encrypt）
sudo certbot --nginx -d your-domain.com -d www.your-domain.com

# 证书 90 天过期，设置自动续期
sudo certbot renew --dry-run
```

---

## 六、NGINX 反向代理（把端口号藏起来）

现在你的应用跑在 `http://IP:7860`，别人得记住端口号。用 NGINX 把它变成标准 80/443 端口：

```bash
sudo apt install nginx -y

sudo nano /etc/nginx/sites-available/myapp
```

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 你的 Gradio 应用在 7860
    location / {
        proxy_pass http://127.0.0.1:7860;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Spring Boot 后端在 8888
    location /api/ {
        proxy_pass http://127.0.0.1:8888;
        proxy_set_header Host $host;
    }
}
```

```bash
sudo ln -s /etc/nginx/sites-available/myapp /etc/nginx/sites-enabled/
sudo nginx -t          # 检查语法
sudo systemctl reload nginx
```

**效果**：

```
之前：http://123.456.78.90:7860     ← 又丑又难记
之后：https://your-domain.com       ← 专业
```

---

## 七、Docker 部署（更专业的方式）

你的生命方程式后端已经有 Docker Compose 配置了，这是目前最标准的部署方式：

### 7.1 为什么用 Docker

```
传统方式：
  服务器装 Python → 装依赖 → 装 Java → 装 MySQL → 各种配置冲突

Docker 方式：
  docker compose up -d
  → 3 分钟后，Python + Java + MySQL 全部就绪，互不干扰
  → 换服务器：同样的命令再跑一遍，一模一样
```

### 7.2 服务器上装 Docker

```bash
# 一键安装
curl -fsSL https://get.docker.com | bash
sudo usermod -aG docker $USER   # 不用每次 sudo
```

### 7.3 部署你的 Spring Boot 项目

```bash
git clone 你的项目
cd 你的项目
docker compose up -d
# 完成。三个服务（app + mysql + nginx）全部上线。
```

---

## 八、费用总结

| 项目 | 费用 |
|------|------|
| 轻量服务器 2核2G | ~50元/月 = 600元/年（新用户有折扣） |
| 域名 .com | ~60元/年 |
| 域名 .top | ~9元/年 |
| SSL 证书 | 免费（Let's Encrypt） |
| 备案 | 免费 |
| **合计第一年** | **约 500-700 元** |
| 学生优惠 | 阿里云有学生机，更便宜 |
| 香港节点 | 贵一点但免备案，省 20 天等待 |

---

## 九、你两个项目的部署路线图

### 智慧校园 AI（Gradio 项目）

```
现在就可以上线：

  ① 买阿里云轻量服务器 Ubuntu 2核2G（50元/月）
  ② SSH 连上去 → git clone → pip install → 配 .env
  ③ nohup 启动 → 访问 IP:7860 就能用了
  ④ 想更好看：买域名 + Nginx 代理 + HTTPS
  ⑤ 想更稳定：配成 systemd 开机自启

  预计耗时：第一次 2 小时，熟了 20 分钟
```

### 生命方程式小程序（Spring Boot + 微信小程序）

```
需要多等一步备案：

  ① 买服务器（同上）
  ② 买域名 → 提交备案 → 等待 15-20 天
  ③ 备案通过后：配 DNS → HTTPS → Nginx
  ④ Docker Compose 一键部署后端
  ⑤ 小程序后台把 request 合法域名改成你的 HTTPS 域名
  ⑥ 提交小程序审核 → 上线

  预计耗时：代码部署 1 小时 + 备案等待 20 天
```

---

## 十、一句话总结

> **买服务器就是租台远程电脑，部署就是把代码拷上去跑，域名就是给 IP 地址起个好记的名字，HTTPS 就是给域名上个安全锁。**

你两个项目都做完了，就差最后一步上线。先买一个月服务器试试水，50 块钱的事。
